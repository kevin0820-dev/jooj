<div class="timeline-placeholder">
	<?php if (isset($cl['prof_user']['about']) && !empty($cl['prof_user']['about'])): ?>
		<div style="border-bottom: 1px solid var(--smc-main-border-color);">
			<p class="about-title">
				<?php echo $cl['prof_user']['about']; ?>
			</p>
		</div>
	<?php endif; ?>
	<?php if (isset($cl['prof_user']['questions']) && !empty($cl['prof_user']['questions'])): ?>
		<div class="faq-stl">
			<p style="padding: 10px 5px 15px 0px; font-size: 25px;">
				Frequently Asked Questions
			</p>
		</div>
		<div class="faq-container">
			<?php foreach ($cl['prof_user']['questions'] as $cl['question']): ?>
				<div class="faq-item">
					<div class="faq-title" onclick="toggleFaq(this)">
						<?php echo $cl['question']['title']; ?>
						<span class="arrow">▼</span>
					</div>
					<div class="faq-content">
						<p><?php echo $cl['question']['answer']; ?></p>
					</div>
				</div>
			<?php endforeach; ?>
		</div>
	<?php endif; ?>
</div>


<style>
	.faq-container {
		margin: auto;
		color: var(--cl-secondary-text-color);
	}

	.faq-item {
		border-bottom: 1px solid var(--smc-main-border-color);
	}

	.faq-title {
		line-height: 1.5;
		color: var(--cl-primary-text-color);
		font-size: 15px;
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 15px 15px 15px 0px;
		cursor: pointer;
	}

	/* .faq-item:hover {
		background-color: var(--smc-main-bg-color-2);
	} */

	.faq-content {
		padding: 22px 15px 15px 0px;
		display: none;
		border-top: 1px solid var(--smc-main-border-color);
	}

	.faq-title .arrow {
		transition: transform 0.1s ease;
	}

	.faq-item.active .arrow {
		transform: rotate(180deg);
	}

	.faq-item.active .faq-content {
		word-wrap: break-word;
		line-height: 1.5;
		font-size: 15px;
		display: block;
	}

	.faq-item.active .faq-title {
		padding: 15px 15px 15px 0px;
	}

	.timeline-placeholder {
		padding: 0px 25px 0px 25px !important;
	}

	.about-title {
		color: var(--cl-secondary-text-color);
		line-height: 1.5;
		padding: 0px 5px 15px 0px;
	}

	.faq-stl {
		padding-top: 20px;
		padding-bottom: 10px;
		color: var(--cl-primary-text-color);
	}

	.pl-message p {
		text-align: center !important;
	}

	.pl-message h4 {
		text-align: center !important;
	}
	.company-overview{
		border-top: 1px solid var(--smc-main-border-color);
		padding-top: 25px;
		color: var(--cl-primary-text-color);
		font-size:25px; 
		padding-bottom:20px; 
	}
</style>
<script>
	function toggleFaq(element) {
		const faqItem = element.parentNode;
		faqItem.classList.toggle("active");
	}
</script>